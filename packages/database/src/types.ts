/**
 * Client-safe type exports
 * This file only exports TypeScript types, no runtime code
 * Safe to import in Client Components
 */

export type {
  Task,
  TaskStatus,
  TaskPriority,
  User,
  FocusSession,
} from "@prisma/client";
