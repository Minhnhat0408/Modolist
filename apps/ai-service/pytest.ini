[pytest]
asyncio_mode = auto
addopts =
    --cov=config
    --cov=database
    --cov=rag
    --cov=estimation
    --cov=task_generator
    --cov=servicer
    --cov=main
    --cov-fail-under=80
    --cov-report=term-missing
